services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.devmachine
    volumes:
      - ../..:/xyz:cached
    # Vigtigt for Playwright i containere:
    cap_add:
      - SYS_ADMIN
    command: sleep infinity
    networks:
      - internal

networks:
  internal:
    driver: bridge
